# 人生模拟 · Hy3 版 —— 端到端 Demo 录制脚本

> 配套 README 第五节。每个 demo 控制在 **≤ 2 分钟**。录屏工具任选（OBS / QuickTime / 浏览器录屏插件），建议 1280×800 或 1920×1080、系统声音 + 麦克风旁白。
> 通用前置：浏览器开一个干净的隐身窗口，避免其它标签页干扰。

---

## Demo 1 · 玩法全流程（无需 Key，本地叙事即可）

目的：展示「卡牌肉鸽 + 人生模拟」完整可玩闭环，证明至少有 1 个可交互前端。

| 镜号 | 画面 / 操作 | 旁白（口播） | 时长 |
|---|---|---|---|
| 1 | 打开 `index.html`（方式 A 纯本地）。标题「人生模拟 · Hy3 版」+ 顶部五维进度条 + 卡牌抽屉。 | 大家好，这是用 Hy3 驱动的「人生模拟」——一款卡牌肉鸽人生游戏。 | 0:00–0:10 |
| 2 | 点开局「选择词条」，出现 3 个词条（如「锦鲤体质」），点「换一组」演示可无限刷新，选 1 个 → 点「开始这一世」。 | 开局先选一个词条，它会影响整局人生的抽卡运气。 | 0:10–0:25 |
| 3 | 童年阶段：点 5 张卡背逐张翻面（健康/财富/关系/快乐/智慧），每张选一个具体行为（如「晨跑」「兼职」）。 | 每个阶段抽五维卡各一张，我要为每张做有取舍的抉择——花财富换健康、结婚增关系却耗精力。 | 0:25–0:55 |
| 4 | 5 张选完后点「撰写本章人生」→ 看到详细长篇人生实时出现在进度条下方（本地规则生成）。 | 抉择做完，系统把五件事织成一段人生章节。这里先演示无 Key 的本地叙事。 | 0:55–1:15 |
| 5 | 依次走完 少年/青年/中年/暮年（可加速点过），80 岁落幕 → 弹出 S~D 评级 + 五维总览。 | 走完五个阶段，80 岁落幕，给出 S 到 D 的永久评级。 | 1:15–1:40 |
| 6 | 打开「人生档案库」→ 点「第 1 世」查看当世五维进度条 + 简略传记。 | 每一世都会存进档案库，可以回看。下面我们看真正接入 Hy3 后的效果。 | 1:40–2:00 |

**收尾提示**：Demo 1 用本地叙事即可，重点在玩法；AI 叙事留到 Demo 2 实证。

---

## Demo 2 · Hy3 真·AI 叙事（必须走本地代理）

目的：实证「全链路通过 API 调用 Hy3」，不是模板/预生成。

> ⚠️ 录制前务必：在本目录执行 `node proxy.js`，并用 **`http://localhost:8787`** 打开游戏（不要用线上 https 链接，否则混合内容会被浏览器拦截）。

| 镜号 | 画面 / 操作 | 旁白（口播） | 时长 |
|---|---|---|---|
| 1 | 终端展示 `node proxy.js` 启动日志：「打开游戏：http://localhost:8787」。 | 因为 TokenHub 不允许浏览器直连，我们用一个本地代理转发接口。 | 0:00–0:12 |
| 2 | 浏览器打开 `http://localhost:8787`，点右上角 ⚙ → 在 API Key 处粘贴 TokenHub Key（可打码后半段）→ Base URL 已是 `http://localhost:8787/v1`、Model `hy3`。 | 在设置里填入我的 Hy3（TokenHub）密钥，模型名 hy3。 | 0:12–0:30 |
| 3 | 点「🔌 测试连接」→ 状态变为「✅ 连接成功，Hy3 可用」。 | 点测试连接，返回成功，说明密钥和接口都通了。 | 0:30–0:42 |
| 4 | 开局选词条 → 进入童年阶段，抽 5 卡翻面抉择（同 Demo1 镜3 操作）。 | 同样抽五维卡、做取舍抉择，这次让 Hy3 来写。 | 0:42–1:05 |
| 5 | 点「撰写本章人生」→ 特写界面「🤖 Hy3 撰写中」标识 + 由 Hy3 生成的大段人生章节正文（滚动展示文风）。 | 看，这一整段人生章节是 Hy3 实时生成的，文风连贯、贴合我的每个抉择。 | 1:05–1:35 |
| 6 | 再走一阶段，对比可见第二章文风不同、细节随抉择变化。结尾说「证明是 Hy3 实时生成，而非固定模板」。 | 换一章文风又不一样——这证明是 Hy3 在实时创作，不是套模板。 | 1:35–2:00 |

**关键镜头别剪掉**：① 测试连接成功的 ✅；② 界面「🤖 Hy3 撰写中」；③ 大段由模型生成的连贯正文。这三处是「真·API 调用」的最有力证据。

---

## 录制小贴士
- 若想对比「有 Key / 无 Key」：Demo 2 镜 2 不填 Key 跑一次（会回退本地叙事），再填 Key 跑一次，剪在一起最直观。
- 密钥出现在屏幕时可打码（如只露 `sk-E8TF…`）再上传，避免泄露。
- 视频命名：`demo1-gameplay.mp4`、`demo2-hy3.mp4`，上传后把链接填进 `SUBMISSION_TEXT.md` 的 PR 描述占位处。
